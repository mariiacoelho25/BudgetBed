<header class="navbar">
  <div class="small-logo">
    <%= link_to root_path do %>
      <%= image_tag 'logo', alt: 'Logo', style: 'max-width: 90px;' %>
    <% end %>
  </div>
  <ul class="nav-links">
    <% if user_signed_in? %>
  <%= link_to "Mes réservations", hotel_room_reservations_path(@hotel, @room), class: 'primary-button' %>

      <%= link_to "Déconnexion", destroy_user_session_path, data: {turbo_method: :delete}, class: 'secondary-btn' %>
    <% else %>
      <%= link_to 'Connexion', new_user_session_path, class: 'primary-button' %>
    <% end %>
  </ul>
</header>
